# 文档纠错回归样本（pycorrector）

目的：验证 `pycorrector` 参与时的命中率、回退率与误报率，避免“文档检查不出来”回归。

## 1. 使用方式

1. 在插件中打开 Python 引擎（`engineMode=python` 或 `hybrid`，并确保 `pythonEngineEnabled=true`）。
2. 逐条复制样本文本到临时笔记，触发检测。
3. 记录结果到文末“回归记录模板”。

## 2. 基础错别字样本（应命中）

1. 今天天齐不太好，出门记得带伞。
2. 这个方案因该可以上线。
3. 他己经完成了主要开发。
4. 我们需要在接再厉，按步就班推进。
5. 这副眼镜是昨天去配眼睛时拿到的。

## 3. 易误报样本（应尽量不误报）

1. 今天天气很好，适合散步。
2. 这个术语是项目内约定词，不应随意替换。
3. 代码示例：`const token = "天齐";`（行内代码不应检）
4. 公式：`$E=mc^2$`（公式不应检）
5. 链接：https://example.com/天齐（URL 不应检）

## 4. 长文样本（稳定性）

复制不少于 2000 字的真实文档，包含：

1. 普通正文
2. frontmatter
3. 代码块
4. 行内代码
5. 公式
6. URL/Markdown 链接

验收要点：

1. 不出现长时间 `pending` 卡死。
2. 面板可见 `request_id`、`engine_source`、`stage_durations`。
3. 若回退到 fallback，面板出现“检测质量可能下降”提示。

## 5. 回归记录模板

| 日期 | 分支/版本 | 样本类型 | 总样本数 | 命中数 | 误报数 | `engine_source=pycorrector` 占比 | fallback 触发率 | 备注 |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| 2026-03-09 | local | 基础错别字 | 5 |  |  |  |  |  |
| 2026-03-09 | local | 易误报 | 5 |  |  |  |  |  |
| 2026-03-09 | local | 长文样本 | 1 |  |  |  |  |  |
