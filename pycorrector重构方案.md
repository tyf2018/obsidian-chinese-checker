---
修改时间: 2026-03-09 12:35:00
版本: v3.1（修订版）
状态: 执行中（按阶段门禁推进）
适用范围: obsidian-chinese-checker 当前仓库（main.js + python_engine_service.py）
---

# 1. 结论

当前插件的主要问题不是 pycorrector 本身，而是“调用链路稳定性”和“状态表达一致性”。

1. 协议层曾出现 CORS/preflight 失败，导致服务在线但前端不可调用。
2. 启动与门控状态收敛不稳定，出现长时间 pending 与误回退。
3. 错误分类不一致，临时网络抖动被渲染为引擎异常。
4. 在 fallback 模式下，文档纠错效果明显下降，用户感知为“检查不出来”。

目标是先把“可调用性和可诊断性”做稳定，再提升文档场景纠错效果。

---

# 2. 当前状态矩阵（避免状态冲突）

## 2.1 已完成

1. 服务端已补 CORS 响应头与 `OPTIONS` 处理（针对 Obsidian `app://` 场景）。
2. 已处理多实例监听风险（前端启动策略与服务端端口独占约束）。
3. 已修正部分错误分类与提示文案（避免 `unknown` 误导）。

## 2.2 待验证（必须通过门禁后才视为完成）

1. 长时间实时检测（>=30 分钟）下，门控不再出现“无限 pending”。
2. `signal_is_aborted_without_reason` 等临时错误不再展示为“引擎异常”。
3. 在常见文档样本中，pycorrector 参与率与命中率达到目标。

## 2.3 未开始

1. 可检测范围抽取（RangeExtractor）专项回归集。
2. 协议契约自动化测试（字段级）。
3. 模块化拆分与规则单一数据源。

---

# 3. 设计缺陷分析

## 3.1 协议层缺陷

1. 未把桌面端跨域约束视为必测项。
2. `/health` 与 `/check` 错误语义缺乏稳定契约，前端依赖字符串猜测。

## 3.2 生命周期缺陷

1. 探活、拉起、门控收敛没有形成严格闭环。
2. 启动阶段缺少明确“终态保证”（ready/unavailable/failed）。

## 3.3 状态机缺陷

1. `loading/init/unavailable/ready` 与 `pycorrector_available` 映射历史上存在偏差。
2. 错误字段有残留污染风险（旧错误覆盖新状态）。

## 3.4 质量层缺陷

1. fallback 与 pycorrector 的结果质量差异没有显式告知。
2. 可检测范围策略（frontmatter、代码块、链接、公式）缺少回归保障，易漏检。

## 3.5 可观测性缺陷

1. 缺少请求级 Trace ID 与分段耗时。
2. 面板虽有回退原因，但仍不足以快速定位失败阶段。

---

# 4. 改造目标（修正后口径）

1. 调用可用性：前端对本地服务请求成功率（非 5xx/网络失败）>= 99%。
2. 引擎真实性：在 `pycorrector_status=ready` 条件下，`engine_source=pycorrector` 占比 >= 95%。
3. 状态一致性：不再出现“health=ready 但面板长期显示启动中/不可用”。
4. 文档效果：标准回归样本中，pycorrector 模式命中率显著高于 fallback，并且不低于当前基线。
5. 可诊断性：任一失败在 1 分钟内可定位到协议、进程、门控或范围抽取阶段。

---

# 5. 改造清单（按优先级）

## P0 可用性止血（先稳住调用）

### P0-1 协议稳定化

- [x] 固化 CORS 与 `OPTIONS` 的自动化冒烟测试。
- [x] 将 `GET /health`、`OPTIONS /check`、`POST /check` 纳入启动后自检脚本。

验收标准：Obsidian 控制台无 CORS/preflight 报错。

### P0-2 单实例与门控收敛

- [x] 后台启动统一走“先探活再拉起”。
- [x] 门控必须在阈值内收敛到终态（不能无限 pending）。
- [x] 输出门控等待时长与尝试次数。
- [x] `/check` 请求超时改为按文本长度自适应，降低长文场景误超时回退。

验收标准：
1. 插件托管的 Python 服务实例保持单活。
2. 10 分钟采样窗口内，监听冲突率 <= 1%。
3. 启动门控 P95 收敛时间 <= 12 秒。

### P0-3 错误分类统一

- [x] transient 错误集合统一：aborted/timeout/fetch_failed。
- [x] transient 映射为“启动中或暂不可达”，不进入 `python_error`。
- [x] `pycorrector_error` 为空时清理历史残留。
- [x] 区分“启动态超时”和“运行态超时”：`pythonStartupGateDone=true` 后，`python_check_timeout/python_health_timeout` 不再映射为 `python_booting`。
- [x] 修复 Obsidian 运行环境下相对模块加载问题：范围抽取逻辑内联到 `main.js`，避免 `require('./range_extractor')` 导致启用失败/降级。

验收标准：`signal_is_aborted_without_reason` 不再以“引擎异常”展示。

---

## P1 检测质量（解决“文档检查不出来”）

### P1-1 模式透明化

- [x] 结果面板显示 `engine_source` 与回退层级（network/startup/unavailable）。
- [x] fallback-only 场景展示“检测质量降级”提示。

验收标准：用户可明确判断本次结果是否由 pycorrector 产生。

### P1-2 文档回归样本集

- [x] 建立 `docs/regression_cases.md`（常见错别字、易误报、长文样本）。
- [ ] 每次改动记录：pycorrector 命中率、fallback 触发率、误报率。

验收标准：回归不通过禁止发布。

### P1-3 可检测范围专项

- [x] 新增范围抽取测试集：
  - frontmatter
  - 代码块/行内代码
  - 公式
  - URL/链接
  - 普通正文
- [x] 对每类样本给出“应检/不应检”断言。

验收标准：范围抽取正确率 >= 99%，且正文不误过滤。

### P1-4 诊断可解释化

- [x] 诊断新增字段：`request_id`、`stage_durations`、`engine_source`。
- [x] 提供“一键复制诊断快照”。

验收标准：单次失败可直接定位阶段，不再靠猜。

---

## P2 结构重构（在 P0/P1 稳定后推进）

### P2-1 前端拆分

- [ ] 从 `main.js` 抽离 `python_client`、`startup_gate`、`result_panel`、`diagnostics`。

### P2-2 协议契约化

- [ ] 更新 `docs/protocol.md` 为字段级契约。
- [ ] 增加契约测试脚本并纳入发布前检查。

### P2-3 规则单一数据源

- [ ] JS fallback 与 Python fallback 共用规则文件。
- [ ] 规则变更必须附带对比测试结果。

验收标准：结构变更不降低 P0/P1 指标，且同类回归频率下降。

---

# 6. 指标与口径（修正）

1. 服务调用成功率：`/health`、`/check` 成功响应占比。
2. pycorrector 参与率：`engine_source=pycorrector` 占比（仅在 ready 条件下统计）。
3. 非主动回退率：非用户禁用情况下回退到 fallback 的比例。
4. 门控收敛指标：平均/P95 收敛时间与超时次数。
5. 范围抽取正确率：应检文本命中 + 不应检文本过滤准确率。
6. 文档命中率：回归文档中预期错误被命中的比例。

---

# 7. 阶段门禁与工期

1. 阶段 A（P0）：2 天。
2. 阶段 B（P1）：2-3 天。
3. 阶段 C（P2）：按资源排期。

门禁规则：
1. P0 完成后需连续稳定运行 48 小时再进入 P1。
2. P1 完成后回归集必须连续 2 轮通过才进入 P2。

---

# 8. 回滚策略（修正为可执行）

当前仓库尚未完整落地方案中的全部特性开关，因此回滚策略调整为：

1. 提交级回滚：按功能提交最小粒度回退。
2. 配置级回滚：
   - 临时切 `engineMode=js` 保证可用。
   - 关闭 `pythonAutoStart` 做隔离排查。
3. 诊断保留：回滚不删除日志与诊断快照。

后续若引入稳定开关，再升级为“开关级回滚优先”。

---

# 9. 发布前检查清单

- [ ] CORS/preflight 自动化冒烟通过。
- [ ] 单实例与门控收敛指标达标。
- [ ] transient 错误不再误报为引擎异常。
- [ ] 范围抽取专项回归通过。
- [ ] 文档回归样本命中率达标。
- [ ] 诊断快照可定位失败阶段。

---

# 10. 本次修订说明

本次修订主要解决上一版的四个欠妥点：

1. 修正“已完成与待办冲突”的状态表达。
2. 修正指标口径，避免用 `/check` 200 误代替 pycorrector 真实生效。
3. 修正回滚策略，使其与当前仓库能力一致。
4. 补充“范围抽取专项”以覆盖文档漏检核心场景。
